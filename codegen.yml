overwrite: true
schema: './src/schema/schema.graphql'
generates:
  src/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
config:
  contextType: '../config/createApolloServer#ApolloContextValue'
